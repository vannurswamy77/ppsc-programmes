// 6. Arithmetic Operations (Reconstructed based on common usage for the variables)
#include <stdio.h>
int main ()
{
    int x, y;
    scanf("%d %d", &x, &y);
    int Difference = x-y;
    int Quotient = x/y;
    printf("Difference: %d\n", Difference);
    printf("Quotient: %d\n", Quotient);
}